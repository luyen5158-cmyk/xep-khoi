# 基礎設施決定記錄

*《疊方塊》Xếp Khối — 最後更新 2026-09-03*

這份文件記錄「東西要放哪裡、用什麼做」的決定，以及**為什麼這樣選、否決了什麼**。
遊戲規則、分數公式、美術規格在 [ho-so-thiet-ke.html](ho-so-thiet-ke.html)，兩份不重疊。

日後想改任何一項之前，先讀這裡的理由 —— 當初否決掉的選項，通常是因為某個到現在還存在的原因。

---

## 一句話總結

**一個公開的、可安裝的網頁遊戲，帶全球排行榜，不放廣告，每月 0 元。**

---

## 決定總表

| 項目 | 決定 | 狀態 |
|---|---|---|
| 遊戲網頁 | GitHub Pages，網址不變 | ✅ 已上線 |
| 程式結構 | 多個 JS 檔，不用打包工具 | ✅ 已完成 |
| 存在形式 | 可安裝的網頁（PWA） | ✅ 已完成 |
| 玩家範圍 | 公開給陌生人玩 | — |
| 排行榜 | 要做，用 Supabase | ⬜ 未開始 |
| 防作弊 | 伺服器重跑整局自己算分 | 🟡 客戶端已備妥 |
| 玩家身分 | 暱稱，不用登入 | ⬜ 未開始 |
| 榜單種類 | 今日 / 本週 / 總榜 | ⬜ 未開始 |
| 名字審核 | 先不做 | — |
| 賺錢 | 不 | — |
| 網域名稱 | 未定，晚點再談 | ⬜ |
| 訪客統計 | Cloudflare 網頁統計 | ⬜ 未安裝 |

---

## 各項決定與理由

### 1. 遊戲網頁 → GitHub Pages

網址 `https://luyen5158-cmyk.github.io/xep-khoi/game/`，推上 `main` 分支就自動更新。

**為什麼：** 本來就在用，不用搬家，免費，而且遊戲本身是純靜態檔案，它做得到的事已經全部夠用。

**否決了：**
- *Cloudflare Pages* —— 在越南比較快，而且能直接接後端。但既然排行榜決定用 Supabase，「同一個後台管到底」這個好處就沒了，剩下的速度差異不值得為它搬家。
- *Vercel / Netlify* —— 免費額度對流量的限制比較緊。

**要注意：** GitHub Pages 只能放死檔案，**它永遠不可能自己撐起排行榜**。這不是設定問題，是它本來就不做這件事。所以排行榜一定得放在別的地方。

### 2. 程式結構 → 多個 JS 檔，不用打包工具

`game/js/` 底下一個檔案負責一件事，瀏覽器用 `<script type="module">` 自己組起來。

**為什麼：** 拿到了拆檔案的好處（照設計文件的 8 個 script 分工，出錯時知道要開哪個檔），卻不用裝 Node.js、不用跑 build 指令、不用改變部署方式 —— 照樣 push 上 GitHub 就好。

**否決了：**
- *單一 HTML 檔* —— 原本的 demo。超過 1500 行之後找東西太痛苦。
- *加上 Vite* —— 能壓縮檔案、改程式自動更新畫面。但要裝 Node.js、每次發佈要跑指令，而且 **repo 放在 iCloud Drive 裡**，一旦有了 `node_modules`（幾萬個小檔案）iCloud 會同步到崩潰。等真的需要壓縮時再加，那時候程式一行都不用改。

**代價（要知道）：** 用 ES 模組之後，**直接雙擊 `game/index.html` 開檔案是不會動的**（瀏覽器的安全限制）。本機要測必須開一個小伺服器：

```bash
python3 -m http.server 8777
```

然後開 `http://localhost:8777/game/`。線上的 GitHub Pages 沒這個問題。

### 3. 存在形式 → 可安裝的網頁（PWA）

`manifest.webmanifest` + `sw.js` + icon。手機瀏覽器選「加到主畫面」就會出現 icon，沒網路也能玩。

**為什麼：** 用零成本換到九成像 App 的體驗。不用付 $25、不用等審核、不用應付商店政策，改版直接 push 就更新。

**否決了：**
- *上架 Google Play* —— 要 $25、要應付審核。如果哪天決定要，用 TWA 或 Capacitor 把現在這個網頁包起來就行，**不需要重寫**。
- *用 Unity 重做* —— 效能和手感最好，但要從頭學 Unity，現有的網頁程式全部作廢。設計文件原本是這個方向，等網頁版證明不夠用再說。

**Service Worker 用「網路優先」策略：** 有網路時永遠拿最新版，沒網路才用存下來的。這是為了避開最經典的坑 —— 玩家卡在舊版本，你明明改好了他卻看不到。發新版時記得改 `sw.js` 裡 `CACHE` 的版本號。

### 4. 排行榜 → 要做，用 Supabase

**分工：網頁在 GitHub Pages，排行榜資料在 Supabase。** Supabase 是資料庫，不是網站主機，這兩件事不衝突。

**否決了：**
- *Cloudflare Workers + D1* —— 免費額度更寬、更不會休眠。但要自己寫比較多東西。
- *完全不做排行榜* —— 一度是預設選項，後來改成要做。

**兩個必須處理的坑：**

**（a）免費專案大約一週沒人用會被自動暫停。** 暫停時排行榜壞掉，但遊戲照常能玩（因為遊戲是死檔案）。解法：設一個每週自動連線一次的排程，免費，設定一次不用再管。

**（b）給瀏覽器用的金鑰是公開的，任何人看原始碼都拿得到。**
所以 **絕對不能讓瀏覽器直接把分數寫進資料庫**，那等於門沒鎖。正確做法在下一條。

### 5. 防作弊 → 伺服器重跑整局自己算分

**問題：** 一旦公開給陌生人玩，只要有人打開開發者工具，就能把分數改成九億分坐在第一名。排行榜第一天就會被毀掉。這是「一定會發生」，不是「可能會發生」。

**做法：** 客戶端送上去的不是分數，而是**整局的下法**（亂數種子 + 每一步放在哪）。伺服器拿到後從頭跑一遍，自己算出分數再寫進資料庫。客戶端傳來的任何數字都不採信。

**為什麼這個遊戲做得到：** 它沒有計時、沒有物理運算、沒有種子以外的隨機，所以整局可以完整重現。這是設計上的運氣，要善用。

**已經備妥的部分：**

| 檔案 | 作用 |
|---|---|
| `game/js/rng.js` | 有種子的亂數，取代 `Math.random()` |
| `game/js/replay.js` | 記錄種子和每一步 |
| `game/js/sim.js` | 重跑整局算分 |

`sim.js` 以及它用到的 `grid.js` / `tray.js` / `score.js` / `level.js` / `shapes.js` **完全沒有用到瀏覽器功能**，所以可以原封不動搬進 Supabase Edge Function 當驗證器。動這幾個檔案時千萬別加入任何 `document`、`window`、`localStorage`。

**已驗證：** 自動玩完整一局，畫面顯示 1507 分，用 replay 重跑算出 1507 分，完全一致。遊戲結束時程式會自動做這個比對，不一致就在 console 出警告。

**資料庫權限必須設成：所有人可讀，沒有人可以直接寫。** 寫入只能透過 Edge Function。

### 6. 玩家身分 → 暱稱，不用登入

自己輸入名字 + 一組存在瀏覽器裡的裝置代碼。

**為什麼：** 摩擦最小，陌生人點進來就能玩。看到「請先登入」很多人直接關掉。

**代價：** 名字會重複、換手機成績帶不走。接受。

**否決了：** *Google 登入* —— 名字不重複、成績跟著人走，但會擋掉大量玩家，而且一旦收集帳號資料就必須寫隱私權政策。

### 7. 榜單種類 → 今日 / 本週 / 總榜

**為什麼不只做總榜：** 一兩個月後總榜前十名就固定不動了，新玩家看一眼知道自己沒希望，就不玩了。有每日榜，今天進來的人今天就有機會上榜。

### 8. 名字審核 → 先不做

**已知風險：** 公開排行榜出現髒話名字是必然，不是可能。這是明知風險下的決定。

**已做的最低成本準備：** 名字限制長度（最多 12 字），後台手動刪除的路留著。哪天要加自動過濾，不用重做。

### 9. 賺錢 → 不做

純興趣。沒有廣告，體驗最乾淨，也不用處理稅務和隱私權政策。

**若之後改變主意：** 網頁廣告收入極低，要包成 App 放 AdMob 才划算 —— 那會反過來影響第 3 條的決定。

### 10. 網域名稱 → 未定

目前是 `luyen5158-cmyk.github.io/xep-khoi/game/`，陌生人記不住。買一個大約一年台幣 300–500 元。**不急，隨時能換。**

### 11. 訪客統計 → Cloudflare 網頁統計（還沒裝）

免費，不用 cookie，所以**不需要那個煩人的同意彈窗**。

---

## 費用

**每月 0 元。**

| 項目 | 費用 |
|---|---|
| GitHub Pages | 0 |
| Supabase 免費方案 | 0 |
| Cloudflare 網頁統計 | 0 |
| 網域（可選，未買） | 約 NT$300–500 / 年 |

需要註冊的新帳號只有 **Supabase** 一個。

**但有一個看不見的成本：** 排行榜上線後就是一個要一直維護的東西。它壞掉時玩家會看到錯誤，你得去修。純靜態網頁沒有這個問題。

---

## 目前進度

**已完成**
- 遊戲網頁上線可玩：https://luyen5158-cmyk.github.io/xep-khoi/game/
- 21 個檔案，按設計文件的 8 個 script 分工拆好
- PWA 可安裝、可離線，Service Worker 在正式網址確認已啟動
- 防作弊地基：有種子的亂數、下法記錄、重跑驗證程式
- 自動玩完整一局測試通過，分數與重跑結果完全一致

**還沒做**
- 暱稱輸入介面
- 排行榜畫面
- Supabase：資料庫、Edge Function 驗證、防休眠排程
- 網域、訪客統計

**還沒驗證**
- 真人用手指在實機上拖放的手感。目前只用程式模擬觸控測過，幾何計算正確，但「順不順手」測不出來。

---

## 動手之前先看這裡

- 改 `sim.js` 及它引用的檔案時，**不要加入任何瀏覽器 API**，否則伺服器端驗證會壞掉。
- 發佈新版時記得改 `game/sw.js` 裡 `CACHE` 的版本號，否則玩家會拿到舊檔案。
- **repo 放在 iCloud Drive 裡。** 不要在這裡產生 `node_modules` 或 build 輸出，iCloud 同步幾萬個小檔案會出問題。真的要用打包工具，先把 repo 搬到 `~/Projects/`。
- 分數公式、等級門檻等所有數字以 [ho-so-thiet-ke.html](ho-so-thiet-ke.html) 為準，程式碼裡的值都跟它一致。
